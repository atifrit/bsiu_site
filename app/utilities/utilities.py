def charComparisonUtility(char1, char2):
   code1 = ord(char1)
   code2 = ord(char2)
   
    


def letterOrderer(post, above, below):
    if not below:

    if not above:

    if len(above) == len(below):
        aboveChar = above[len(above)-1]
        belowChar = below[len(below)-1]

        comparVal = stringComparisonUtility(aboveChar, belowChar)
    